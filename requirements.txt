# --- Agent orchestration ---
langgraph>=0.2.50
langchain-core>=0.3.0

# --- LLM provider (Groq for both 70B primary and 8B fallback) ---
langchain-groq>=0.2.0
groq>=0.11.0

# --- HTTP client - pinned for groq compatibility ---
# (httpx>=0.28 removed the 'proxies' kwarg used by older groq)
httpx==0.27.2

# --- Structured outputs ---
pydantic>=2.5.0

# --- Researcher tools ---
tavily-python>=0.5.0
arxiv>=2.1.0
PyGithub>=2.4.0

# --- Caching, env, async helpers ---
diskcache>=5.6.0
python-dotenv>=1.0.0

# --- UI ---
streamlit>=1.40.0

# --- Tracing (optional) ---
langsmith>=0.1.0

# --- Tests ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
